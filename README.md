This project is a proof-of-concept Python script that monitors Ableton .als project files in a specified folder.
It logs file edit events (timestamp, file hash, file size) and tracks user session activity with automatic idle protection.
With the rise and debates surrounding AI-generated music, this could be a feature seen implemented in DAWS in the future, or something similar.
